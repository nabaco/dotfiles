#
# ~/.bash_profile
#

[[ -f ~/.profile ]] && . ~/.profile
# No need for that as it is started from ~/.profile
# [[ -f ~/.bashrc ]] && . ~/.bashrc

[[ $(tty) == "/dev/tty1" ]] && startx
. "$HOME/.cargo/env"
